# Kafka & Clickhouse with Fraud transactions data
Датасеты предоставлены в рамках соревнования https://www.kaggle.com/competitions/teta-ml-1-2025

## ✏️ Структура проекта

```
.
├── docker-compose.yml      # Инфраструктура (Kafka, ZK, ClickHouse, AKHQ)
├── requirements.txt        # Библиотеки для Python
├── producer.py             # ETL cкрипт загрузки данных
├── sql/
│   ├── create_tables.sql   # Создание таблиц в Clickhouse, получающие данные из топика (Оптимизированная версия)
│   └── analysis.sql        # Аналитический запрос
├── README.md               # Вы тут
└── data/                   # Здесь должен лежать файл с любым именем, но расширением .csv
    └── test.csv           
```
## 🔌 Занятые порты

Развернутая инфраструктура использует следующие порты:

| Сервис | Порт | Назначение |
| :--- | :--- | :--- |
| **AKHQ** | `8080` | Веб-интерфейс для просмотра топиков и сообщений Kafka. Доступен в браузере: http://localhost:8080 |
| **ClickHouse (HTTP)** | `8123` | HTTP API. Используется для выполнения SQL-запросов через `curl` или HTTP-клиенты. |
| **ClickHouse (Native)**| `9000` | TCP порт (Native Protocol). Используется для подключения через IDE (DBeaver, DataGrip) или `clickhouse-client`. |
| **Kafka** | `9092` | Брокер сообщений. Сюда подключается Python-скрипт (`producer.py`) для отправки данных. |
| **Zookeeper** | `2181` | Служебный порт для координации работы кластера Kafka. |

## Что делает сервис
1. Загрузка данных из csv файла в топик Kafka
2. Формирование таблиц в Clickhouse получающие данные из топика в таблицу и оптимзация хранения данных, чтобы запросы испольнялись быстрее
3. Формирование sql запроса для получения категории наибольшей транзакции по каждому штату (отдельный sql запрос + результирующий файл csv)
4. Результирующий файл csv

## 🚀 Быстрый старт

### Требования
- Docker 20.10+
- Docker Compose 2.0+

### Запуск

Скачайте файл `train.csv` или `test.csv` (лучше `test.csv`, с ним будет быстрее) из соревнования https://www.kaggle.com/competitions/teta-ml-1-2025 и разместите в директории `/data`
```bash
git clone https://github.com/uroplatus666/mts25_mlops_hse_3part.git
cd mts25_mlops_hse_3part

# Сборка и запуск всех сервисов
docker compose up -d
```

### Создание таблиц в Clickhouse
```bash
cat sql/create_tables.sql | docker exec -i clickhouse clickhouse-client -u click --password click -n

# Проверка, что таблицы создались
curl 'http://click:click@localhost:8123/?query=SHOW%20TABLES'
```
### Создание виртуальной среды и установка зависимостей
```bash
python3 -m venv .venv 
source .venv/bin/activate 
pip install -r requirements.txt
```
### Отправка данных в Kafka
```bash
python producer.py
```
### Проверяем дошли ли данные до Clickhouse
```bash
curl 'http://click:click@localhost:8123/?query=SELECT+count()+FROM+transactions'
```
### Делаем нужный запрос, сохраняем результат в result.csv и выводим первые 5 строк файла для проверки
```bash
cat sql/analysis.sql | curl 'http://click:click@localhost:8123/' --data-binary @- > result.csv
head -n 5 result.csv
```
